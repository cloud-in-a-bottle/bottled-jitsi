#!/bin/bash
# Just log and let s6 restart the service. Setting exitcode would
# bring down the whole container, which makes debugging painful and
# produces endless restart loops under OpenHost's supervisor.
echo "[finish] prosody exited (code=$1, signal=$2)" >&2
sleep 2
